"""Vendored qlib + Tushare data collectors (self-contained ingest).

See ``NOTICE.md`` for provenance/licensing. This package exists so the vendored
tree ships in built wheels/sdists (discoverable by setuptools) — the collectors
themselves are still executed as subprocess scripts, not imported here.
"""
